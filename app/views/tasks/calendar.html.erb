<div>
 <%= month_calendar events: @events do |date,events| %>
  <%= date.day %>
  
  <% events.first(3).each do |event| %>
   <div>
    <%= link_to edit_task_path(event.id) do %>
     <%= event.name %>(<%= event.start_time.strftime('%m:%d') %>~<%= event.finish_time.strftime('%m:%d') %>)
    <% end %>
   </div>
  <% end %>
 <% end %>
</div>